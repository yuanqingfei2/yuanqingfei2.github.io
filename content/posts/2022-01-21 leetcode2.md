---
title: Leetcode -- 字符串
date: "2022-01-21T03:07:00.000Z"
---

## 字符串替换

这个没有啥说的，两个方法，

```java
class Solution {
    public String replaceSpace(String s) {
        StringBuffer result = new StringBuffer();
        for(int i = 0; i < s.length(); i ++){
            if(s.charAt(i) == ' '){
                result.append("%20");
            } else {
                result.append(s.charAt(i));
            }
        }
        return result.toString();
    }
}
```

时间O(n)，空间O(n)。

第二种办法直接用API

```java
class Solution {
    public String replaceSpace(String s) {
        if(s == null) return null;
        return s.replaceAll(" ", "%20");
    }
}
```
我猜应该也是时间O(n)，空间O(n)。毕竟扫描和新建一个字符串都不可少。

## 左旋字符串，也就是把前面几个移到后面去

也是两种办法，第一个使用API

```java
class Solution {
    public String reverseLeftWords(String s, int n) {
        return s.substring(n, s.length()) + s.substring(0, n);
        // if(s == null) return null;
        // String head = s.substring(0, n);
        // String tail = s.substring(n);
        // StringBuffer result = new StringBuffer(tail);
        // result.append(head);
        // return result.toString();
    }
}
```
时间应该是O(n)，空间应该是O(n)

第二种就是自己做循环。
```java
class Solution {
    public String reverseLeftWords(String s, int n) {
        if(s == null) return null;
        StringBuilder result = new StringBuilder();
        for(int i = n; i < s.length(); i ++)  result.append(s.charAt(i));            
        for(int i = 0; i < n; i ++) result.append(s.charAt(i));
        return result.toString();
    }
}
```
时间是O(n)，空间是O(n)

还可以求余，这真是个好思路

```java
class Solution {
    public String reverseLeftWords(String s, int n) {
        StringBuilder res = new StringBuilder();
        for(int i = n; i < n + s.length(); i++)
            res.append(s.charAt(i % s.length()));
        return res.toString();
    }
}    
```
